# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=howallet
DB_PASSWORD=howallet_secret
DB_NAME=howallet
DB_SSLMODE=disable

# API
API_PORT=8080
API_HOST=0.0.0.0

# JWT
JWT_SECRET=change-me-to-a-random-secret-at-least-32-chars
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=720h

# Frontend
FRONTEND_URL=http://localhost:3000

# SMTP (optional — invitations work without email, link returned in API response)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Environment
ENV=development
